<Alloy>
	<Label id="acceptedOnLbl" class="margin-top link-wrap" if="!$.args.registrationFlow"/>
	<WebView id="webView" class="margin-top" />
	<View class="auto-height vgroup bottom">
		<Button id="revokeBtn" class="margin-top secondary-btn hide" onClick="didClickRevoke" />
		<Button id="successBtn" class="margin-top margin-bottom primary-btn" onClick="didClickSuccess" />
	</View>
</Alloy> 