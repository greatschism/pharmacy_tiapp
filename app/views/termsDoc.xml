<Alloy>
	<ScrollView id="scrollView" >
		<WebView id="webView" />
		<Button id="emailBtn" class="margin-bottom primary-btn" onClick="didClickEmail" />
		<Button id="revokeBtn" class="margin-bottom tertiary-btn" onClick="didClickRevoke" />
	</ScrollView>
</Alloy> 