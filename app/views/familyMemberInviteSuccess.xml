<Alloy>
	<ScrollView class="vgroup">
		<View class="auto-height">
			<ImageView id="inviteSuccessImg" />
			<Label id="inviteLbl" class="image-title"/>
		</View>
		<Label id="inviteMsg" class="lbl-wrap margin-top margin-bottom"/>
		<Button id="addBtn" class="primary-btn margin-top margin-bottom" onClick="didAddAnotherMember"/>
		<Button id="doneBtn" class="secondary-btn margin-top margin-bottom" onClick="didClickDone"/>
	</ScrollView>
</Alloy>